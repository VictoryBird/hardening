---
# 진입점: discover → deploy → execute
- name: "Discover server characteristics"
  include_tasks: discover.yml

- name: "Deploy hardening scripts"
  include_tasks: deploy.yml

- name: "Execute hardening"
  include_tasks: execute.yml
